apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app.kubernetes.io/name: backend
  ports:
  - name: backend
    protocol: TCP
    port: 8000
    targetPort: backend